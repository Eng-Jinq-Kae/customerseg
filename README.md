# Pip
pip install numpy\
pip install pandas\
pip install openpyxl\
pip install matplotlib\
pip install seaborn\
pip install scikit-learn

# Run
Double-click run.cmd to start the Streamlit app. You must have a default .xlsx marketing dataset in the same directory as app.py.